{% extends 'layout.twig' %}

{% block content %}

	<h3>Заказы</h3><br>

	<table border='1'>
    <tr class="top_row">
        <td>id</td>
        <td>Время создания</td>
    </tr>
	{% for order in orders %}
	    <tr >
	        <td><a href="/admin_orders/details/{{ order.id }}">{{ order.id}}</a></td>
	        <td><a href="/admin_orders/details/{{ order.id }}">{{ order.created_at}}</a></td>
	    </tr>
	{% endfor %}
</table>

{% endblock %}